<html>
<head>
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
</head>

<body>
	<a href="https://localhost/single_love/index.php/Home/Email/Lookemail/index"><h1>邮件系统</h1></a>
  	<div id="content">

	<fieldset>
 		<h2>收件箱(未读):({$count['count']})</h2>
		<for start = "0" end = "$count['count']">
			<fieldset>
				<h4>ID:{$count["email"][$i]["members_id_a"]}</h4>
				<h4>收信时间:{$count["email"][$i]["time_a"]}</h4>
				<a href="https://localhost/single_love/index.php/Home/Email/reademail/index/userid/{$count["email"][$i]["members_id_a"]}/id/{$count["email"][$i]["id"]}"><button type="button"><h4>查看邮件</h4></button></a>
	    	</fieldset>
		</for>
 	</fieldset>
 	<br/>

	<fieldset>
    	<h2>收件箱(已读):({$read['count']})</h2>
		 <for start = "0" end = "$read['count']">
         	<fieldset>
            	<h4>ID:{$read["email"][$i]["members_id_a"]}</h4>
 	           	<h4>读信时间:{$read["email"][$i]["time_b"]}</h4>
				 <a href="https://localhost/single_love/index.php/Home/Email/read/index/userid/{$read["email"][$i]["members_id_a"]}/id/{$read["email"][$i]["id"]}"><button type="button"><h4>查看邮件</h4></button></a>
 	      	</fieldset>
 	   	</for>
    </fieldset>
    <br/>

	<fieldset>
    	<h2>已发送:({$send['count']})</h2>
		<for start = "0" end = "$send['count']">
	        <fieldset>
          		<h4>ID:{$send["email"][$i]["members_id_b"]}</h4>
 				<h4>发送时间:{$send["email"][$i]["time_a"]}</h4>
				<h4>对方:
					<if condition = "$send['email'][$i]['state']"> 已读
					<else/>未读
					</if>
				</h4>
				<a href="https://localhost/single_love/index.php/Home/Email/Send/index/userid/{$send["email"][$i]["members_id_b"]}/id/{$send["email"][$i]["id"]}"><button type="button"><h4>查看邮件</h4></button></a>
 			</fieldset>
 		</for>

    </fieldset>
    <br/>
   	</form>
 </div>

  <div id="footer"></div>
 </body>
</html>

